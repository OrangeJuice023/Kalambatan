version: 2

sources:
  - name: raw
    description: Bronze layer — synthetic telecom data landed as-is from CSVs.
    schema: raw
    tables:
      - name: customers
      - name: plans
      - name: billing
      - name: network_usage
      - name: support_tickets
      - name: retention_actions
      - name: churn_events
